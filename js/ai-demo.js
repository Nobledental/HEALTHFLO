// Hook for small AI demo cards later (kept minimal for performance)
export function fakeAIResponse(q){
  return `Looking up: “${q}” → try Cashless Eligibility (30s) to confirm network availability.`;
}
